
export interface SuperAdminInterface {
    code: string
}



/*export interface SuperAdminElectionInterface {
    constitution: constitution;
    election: election;
    localgovernment: localgovernment;
    parties: parties;
    result: result;
    roles: roles
    state: state;
}

interface constitution{
    id: string;
    name: string;
}

interface election{
    id: string;
    name: string;
}

interface localgovernment {
    id: string;
    name: string;
}

interface parties {
    id: string;
    name: string;
}

interface result {
    id: string;
    name: string;
    count: number;
}

interface roles {
    id: string
    name: string
}

interface state {
    id: string;
    name: string
}*/